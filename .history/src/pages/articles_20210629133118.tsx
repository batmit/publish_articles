export function Articles() {
    return (
        <>
            <header>
                <h1>Published Articles</h1>
            </header>

            await database.ref(`articles/${assunto}`).push(article);

        </>
    );
}